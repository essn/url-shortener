<%= form_for url, remote: true do |f| %>
  <div class="form-group">
    <%= f.label :url, 'Enter a URL to shorten' %>
    <%= f.text_field :url, class: 'form-control', placeholder: "Enter a URL..." %>
    </br>
    <%= f.submit "Submit", class: 'btn btn-default' %>
  </div>
<% end %>
